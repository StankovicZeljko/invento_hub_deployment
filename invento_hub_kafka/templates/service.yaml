apiVersion: v1
kind: Service
metadata:
  name: {{ include "invento_hub_kafka.fullname" . }}
  labels:
    {{- include "invento_hub_kafka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "invento_hub_kafka.selectorLabels" . | nindent 4 }}
