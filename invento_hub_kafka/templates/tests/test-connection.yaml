apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "invento_hub_kafka.fullname" . }}-test-connection"
  labels:
    {{- include "invento_hub_kafka.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "invento_hub_kafka.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
